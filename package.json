{
  "name": "eyir",
  "version": "0.3.0",
  "description": "Discord bot for the Warrior discord server, Skyhold.",
  "main": "eyir.js",
  "repository": "https://github.com/xMarok/eyir",
  "author": "Marok",
  "license": "UNLICENSED",
  "dependencies": {
    "discord.js": "^12.2.0",
    "dotenv": "^8.2.0",
    "gaze": "^1.1.3",
    "moment-timer": "^1.3.0",
    "node-persist": "^3.1.0"
  },
  "scripts": {
    "start": "pm2 start ecosystem.config.js --only eyir",
    "skyhold-test": "pm2 start ecosystem.config.js --only skyhold-test",
    "eyir-test": "pm2 start ecosystem.config.js --only eyir-test",
    "restart": "yarn compile && pm2 restart all",
    "compile": "tsc",
    "format": "prettier --write src"
  },
  "devDependencies": {
    "@types/gaze": "^1.1.0",
    "@types/node": "^14.0.11",
    "@types/ws": "^7.2.6",
    "pm2": "^4.4.0",
    "prettier": "^2.0.5",
    "typescript": "^3.9.5"
  }
}
